# references/motion
Scroll, reveal, hover and transition behaviour. Screen recordings (.mp4/.webm/.gif) are fine
here; they are never shipped on the site. Say what the motion does, e.g.
`ref_nav_island-morph.mp4`: "pill changes width as one object, contents cross-fade".
